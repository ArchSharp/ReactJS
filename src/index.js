import React from "react";
import ReactDOM from "react-dom/client";
import { ArchIntelWebsite } from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <ArchIntelWebsite />
  </React.StrictMode>
);
